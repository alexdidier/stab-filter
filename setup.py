from setuptools import setup, find_packages

setup(
    name="stab_filter",
    version="1.0.0",
    packages=find_packages(),
    description="Python implementation of the Stability Filter algorithm",
)